import { ScrolledToViewDirective } from './scrolled-to-view.directive';

describe('ScrolledToViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolledToViewDirective();
    expect(directive).toBeTruthy();
  });
});
